@page "/"
@page "/GettingStarted"

@inherits MainLayoutComponent
<PageHeader Keywords="@_keywords" SubTitle="Securely Convert Postman Collections into
fully feautured ApiClients for free online, all in the browser. Just a few clicks and your done.
OpenAPI, GraphQL, cURL, RAML, HAR and other specifications can be also converted by first importing into Postman."
             Title="@_title" ></PageHeader>

<BreadCrumbs Items="@BreadCrumbs"></BreadCrumbs>

<br />

<GettingStarted />

@code 
{
    private string _title => Navigate.Value.BaseUrl == Navigate.Value.CurrentUrl ? "Home" : "Getting Started";
    private string _keywords => "openapi, open api, swagger, convert, graphql, curl, raml, har";
    private static readonly List<BreadcrumbItem> BreadCrumbs = new()
    {
        new("Home", "/", true, Icons.Rocket),
    };

    protected override void OnInitialized()
    {
        TabsService.Value.SetHome(null);
    }
}
